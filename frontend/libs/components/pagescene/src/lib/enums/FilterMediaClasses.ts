// Media class filter (audio / image / video / dimensional). Used in
// asset browsing and weight-listing queries.
export enum FilterMediaClasses {
  AUDIO = "audio",
  IMAGE = "image",
  VIDEO = "video",
  DIMENSIONAL = "dimensional",
}
