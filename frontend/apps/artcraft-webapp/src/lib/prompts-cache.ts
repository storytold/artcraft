// Moved to the shared generation-list lib (used by both webapp and desktop).
// Re-exported so every webapp consumer shares the lib's module-level cache.
export { getCachedPrompt, usePrompts } from "@storyteller/ui-generation-list";
