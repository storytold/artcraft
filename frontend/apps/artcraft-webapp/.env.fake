# Loaded by `nx serve artcraft-webapp --mode fake`.
#
# Points the webapp at the local fake-storyteller-web stand-in instead of the
# default dev target, which proxies through to production api.storyteller.ai.
# Start the fake first with `nx serve fake-storyteller-web`.
VITE_DEV_API_HOST=http://localhost:12345
