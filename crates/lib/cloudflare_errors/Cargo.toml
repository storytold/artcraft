[package]
name = "cloudflare_errors"
edition = "2024"
version = "0.0.1"
publish = false

[lib]
name = "cloudflare_errors"
path = "src/lib.rs"

[dependencies]

# Internal
# (None)

# External
# (None)

[dev-dependencies]
# (None)
